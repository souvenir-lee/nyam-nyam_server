const { pruduction } = require('../../models');
module.exports = {
  post: (req, res) => {
    const {
      userId,
      productionName,
      pruductionImg,
      ingrediet1,
      ingredient2,
      info,
      storeId,
      dessertType,
    } = req.body;
  },
};
